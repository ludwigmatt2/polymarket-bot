services:
  bot:
    build: .
    ports:
      - "8765:8765"
    env_file:
      - .env
    volumes:
      - .:/app
    command: python -m uvicorn dashboard.server:app --host 0.0.0.0 --port 8765 --reload --log-level info
    restart: unless-stopped
