# ── Secrets ────────────────────────────────────────────────────────────────────
.env
.env.bak*
*.key
*.pem
# Per-user Polymarket credentials live Fernet-encrypted in config/user_keys.enc.json
# (roles/metadata in config/users.json — never plaintext keys). Never commit config/.
config/

# ── Python ─────────────────────────────────────────────────────────────────────
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# ── Test / coverage ────────────────────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/
*.cover

# ── Logs & runtime data ────────────────────────────────────────────────────────
logs/
*.log

# ── One-time Railway seed data (committed intentionally, remove after migration) ─
!_seed/
!_seed/**

# ── Claude Code — personal config, not for repos ───────────────────────────────
.claude/
CLAUDE.md

# ── macOS ──────────────────────────────────────────────────────────────────────
.DS_Store

# Local secret backups (Phase D) — never commit
backups/
